const progress=document.querySelector('.progress i'),nav=document.querySelector('nav');
function onScroll(){const h=document.documentElement.scrollHeight-innerHeight;progress.style.width=(scrollY/Math.max(h,1)*100)+'%';nav.style.background=scrollY>40?'rgba(7,10,8,.78)':'transparent';nav.style.backdropFilter=scrollY>40?'blur(18px)':'none';document.querySelectorAll('[data-speed]').forEach(el=>{const r=el.getBoundingClientRect();const shift=(innerHeight/2-r.top-r.height/2)*parseFloat(el.dataset.speed);el.style.transform=`translateY(${shift}px)`})}
addEventListener('scroll',onScroll,{passive:true});onScroll();
const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('show')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(e=>io.observe(e));
const c=document.querySelector('.cursor'),c2=document.querySelector('.cursor2');addEventListener('pointermove',e=>{c.style.left=e.clientX+'px';c.style.top=e.clientY+'px';c2.animate({left:e.clientX+'px',top:e.clientY+'px'},{duration:500,fill:'forwards'})});
document.querySelectorAll('a,.project,.btn').forEach(el=>{el.addEventListener('mouseenter',()=>c2.classList.add('big'));el.addEventListener('mouseleave',()=>c2.classList.remove('big'))});
document.querySelectorAll('.magnetic').forEach(el=>{el.addEventListener('pointermove',e=>{const r=el.getBoundingClientRect(),x=(e.clientX-r.left-r.width/2)*.18,y=(e.clientY-r.top-r.height/2)*.18;el.style.transform=`translate(${x}px,${y}px)`});el.addEventListener('pointerleave',()=>el.style.transform='')});
document.querySelector('.menu').addEventListener('click',()=>{const links=document.querySelector('.links'),open=links.style.display==='flex';links.style.display=open?'none':'flex';if(!open){links.style.position='absolute';links.style.top='72px';links.style.right='14px';links.style.flexDirection='column';links.style.padding='18px';links.style.background='#0c110e';links.style.border='1px solid rgba(183,255,99,.14)';links.style.borderRadius='18px'}});
